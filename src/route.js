const express = require('express');
const router = express.router();

router.post('/books',);
router.get('/books',);
router.get('/boos/{id}')
router.put('/books/{id}')
router.delete('/delete/')

module.exports = router